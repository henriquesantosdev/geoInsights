generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model municipality {
  id         String   @id @default(uuid())
  state      String
  name       String
  present   Boolean  @default(false)
  created_at DateTime

  @@map("state")
}
